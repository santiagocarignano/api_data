name: check

on:
  push:

jobs:
  run-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: testing
        run: |
          echo Running test
          echo Test passed